<p style="color: green"><%= notice %></p>
<h1>
<%= House.where(id: @rough.house_id).last.address%>
</h1>
<%= render @rough %>

<div>
  <%= link_to "New rough", new_rough_path, class:"btn btn-outline-dark btn-sm" %> | 
  <%= link_to "Edit this rough", edit_rough_path(@rough), class:"btn btn-outline-dark btn-sm" %> |
  <%= link_to "Back", houses_path, class:"btn btn-outline-dark btn-sm"  %>
</div>
